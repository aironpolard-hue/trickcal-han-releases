# Trickcal 韩服汉化发布文件

公开下载内容：

- Android 一键汉化 APK
- 手动汉化文件
- 手动恢复文件

`trickcal-release.json` 是网站与软件共同读取的唯一公开发布清单。

清单只有软件、汉化资源、恢复资源三项。每项只需维护：

- `version`：版本号
- `url`：原始路线下载链接

文件开头的 `routes` 保存原始路线和备用路线前缀，软件与网站会自动组合使用。
汉化与恢复资源版本统一使用 `yyyy-MM-dd-HH-mm` 格式。
